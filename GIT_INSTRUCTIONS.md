# 🚀 Инструкция по загрузке в GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите **"New repository"**
3. Введите название: `AutomateExcelTasks`
4. Описание: `Excel Report Automation System with Data Summation`
5. Выберите **Public** или **Private**
6. **НЕ** создавайте README, .gitignore, LICENSE (они уже есть!)
7. Нажмите **"Create repository"**

## Шаг 2: Инициализируйте Git локально

Откройте PowerShell в папке проекта и выполните:

```bash
# Инициализация Git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Excel Report Automation System v4.0"

# Переименование ветки в main
git branch -M main
```

## Шаг 3: Подключите удалённый репозиторий

Замените `YOURUSERNAME` на ваше имя пользователя GitHub:

```bash
git remote add origin https://github.com/YOURUSERNAME/AutomateExcelTasks.git
```

## Шаг 4: Отправьте код на GitHub

```bash
git push -u origin main
```

При первом push может запросить логин и пароль GitHub.

## Шаг 5: Проверьте

Откройте https://github.com/YOURUSERNAME/AutomateExcelTasks

Вы должны увидеть:
- ✅ Все файлы проекта
- ✅ README.md отображается на главной странице
- ✅ LICENSE файл
- ✅ .gitignore работает (*.db и uploads/ не загружены)

## 📝 Дальнейшие обновления

После изменений в коде:

```bash
# Проверить изменения
git status

# Добавить изменённые файлы
git add .

# Закоммитить
git commit -m "Описание изменений"

# Отправить на GitHub
git push
```

## 🔐 Важно!

Убедитесь что в `.gitignore` есть:
- ✅ `*.db` - база данных не попадёт в Git
- ✅ `uploads/` - временные файлы не попадут
- ✅ `venv/` - виртуальное окружение не попадёт

## 🎯 Что будет на GitHub?

**Загружается:**
- Весь код (Python, JavaScript, CSS, HTML)
- Шаблон Excel (`static/file/Шаблон.xlsx`)
- Документация (README)
- Лицензия (MIT)
- Скрипты запуска

**НЕ загружается:**
- База данных (*.db)
- Загруженные файлы (uploads/)
- Виртуальное окружение (venv/)
- Временные файлы

## ✅ Готово!

Ваш проект теперь на GitHub и доступен всему миру! 🌍

Другие пользователи смогут:
- Скачать проект
- Установить зависимости
- Использовать систему
- Вносить улучшения

